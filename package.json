{
  "name": "json-fun",
  "version": "1.2.0",
  "description": "Fun with JSON",
  "private": true,
  "type": "module",
  "dependencies": {
    "highlight.js": "^11.9.0",
    "rimraf": "^5.0.5",
    "rxjs": "^7.8.1",
    "tailwindcss": "^3.3.5",
    "typescript": "^5.2.2",
    "vite": "^4.5.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.31",
    "prettier": "^3.1.0",
    "prettier-plugin-tailwindcss": "^0.5.7",
    "vitest": "^0.34.6"
  },
  "scripts": {
    "start": "tsc && vite src/",
    "build": "tsc && vite build --outDir ../dist/ src/",
    "prebuild": "npm run clean",
    "clean": "rimraf ./dist",
    "test": "tsc && vitest"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
